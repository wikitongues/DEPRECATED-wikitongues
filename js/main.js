$(window).load(function(){
  // disableAutofill()
})

$(function(){
  reponsiveIframe()
  openLegal()
  closeModule()
  carousel()
  sectionTabs()
  navScrollWatcher()
  // setTimeout(introAnimate,300)
  // setTimeout(bannerFade,7500)
})

