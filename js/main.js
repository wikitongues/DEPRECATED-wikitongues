$(window).load(function(){
  // disableAutofill()
})

$(function(){
  reponsiveIframe()
  openLegal()
  closeModule()
  carousel()
  sectionTabs()
  dynamicNav()
  navScrollWatcher()
  formInputWatcher()
  textAreaSizeWatcher()
  // setTimeout(introAnimate,300)
  // setTimeout(bannerFade,7500)
})

